<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context=".MainActivity">

    <item android:id="@+id/mic_button"
        android:icon="@drawable/ic_mic_black_24dp"
        android:title="mic_ic"
        app:showAsAction="ifRoom"
        android:orderInCategory="1"
        android:menuCategory="secondary"/>

    <item android:id="@+id/ear_button"
        android:icon="@drawable/ic_hearing_black_24dp"
        android:title="ear_ic"
        app:showAsAction="ifRoom"
        android:orderInCategory="1"
        android:menuCategory="secondary"/>

    <item android:id="@+id/graph_button"
          android:icon="@drawable/ic_timeline_black_24dp"
          android:title="graph_ic"
          app:showAsAction="ifRoom"
          android:orderInCategory="1"
          android:menuCategory="secondary"/>
</menu>
